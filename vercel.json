{
  "buildCommand": "pnpm install --prefer-offline && vite build",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "pnpm install --prefer-offline",
  "devCommand": "vite",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet .",
  "git": {
    "deploymentEnabled": true
  }
}